//string to number
let amountStr = '100'
console.log(amountStr, typeof amountStr);
// amountStr = parseInt(amountStr);
// amountStr = +amountStr
amountStr = Number(amountStr);
console.log(amountStr, typeof amountStr);

//convert number to string
let amountInt = 1000
amountInt = amountInt.toString();
console.log(amountInt, typeof amountInt);
